{
  "name": "mock-server",
  "version": "1.0.0",
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "scripts": {
    "start": "json-server --watch ../mock-data/bd.json --port 3000"
  }
}
